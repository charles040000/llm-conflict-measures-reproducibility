# Figures

PDF and PNG versions are stored by chapter function. Rebuild them with:

```bash
python analysis/generate_figures.py
```

The Taliban pre/post descriptive figures are produced by `analysis/run_taliban_descriptive.py` and are also run by `reproduce.py`.
